# Ciclo de vida de fuentes

Una fuente se agrega a `raw/` solo cuando está aprobada para el proyecto. Se conserva su procedencia, versión, fecha y checksum cuando esté disponible. Las revisiones nuevas se agregan como nuevos artefactos; nunca se modifica retrospectivamente una fuente RAW.
